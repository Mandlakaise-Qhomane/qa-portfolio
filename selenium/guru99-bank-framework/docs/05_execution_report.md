# Execution Report

Fill this after the first successful local or CI run.

- Date:
- Environment: Chrome / Java 17
- Passed:
- Failed:
- Skipped:
- Extent report path: `reports/extent-report.html`

Notes:
- Guru99 manager IDs expire. If login fails, generate a new ID at the Guru99 bank signup page and update `config.properties` only (never hard-code in tests).
- Do not commit `reports/` or `screenshots/` if they grow large; attach a screenshot of the Extent dashboard in README instead.
