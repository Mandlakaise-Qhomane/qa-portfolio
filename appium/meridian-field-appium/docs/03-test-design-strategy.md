# Design

Equivalence (valid/invalid login), state machine (ASSIGNED → IN_PROGRESS → COMPLETED), search, filter, settings persistence, session timeout.

Automation: Appium Java client on the classpath, Page Object (`data-test` locators), Cucumber, TestNG. Gestures on Android are reserved for `-Dplatform=android`.
