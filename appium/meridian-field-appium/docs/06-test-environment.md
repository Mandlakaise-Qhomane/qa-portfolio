# Environment

| Item | Local profile | Android profile |
|---|---|---|
| Driver | Chrome mobile emulation | Appium UiAutomator2 + Chrome |
| App | `LocalFieldApp` :18081 | same URL on the emulator |
| JDK | 17 | 17 |
| Appium server | not required | `appium` on :4723 |

`config.properties` → `platform=localchrome` or `android`.
